<div class="donation-container">
    <div class="donation-header">
        <h2>Palliers de donation</h2>

        <a href="{{ path('donation_goal_new') }}" data-turbo-frame="donation_goal_modal" class="button button-primary">
            {{ include('svg/icons/add-line.svg.twig') }}
        </a>
    </div>

    <turbo-frame id="donation_goal_table">
        {% include 'donation_goal/_table.html.twig' %}
    </turbo-frame>

    <turbo-frame id="donation_goal_modal"></turbo-frame>
</div>
