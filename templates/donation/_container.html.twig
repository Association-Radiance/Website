<div class="donation-header">
    <h2>Donations</h2>

    <a href="{{ path('donation_new') }}" data-turbo-frame="donation_modal" class="button button-primary">
        {{ include('svg/icons/add-line.svg.twig') }}
    </a>
</div>

<turbo-frame id="donation_table">
    {% include 'donation/_table.html.twig' %}
</turbo-frame>

<turbo-frame id="donation_modal"></turbo-frame>
